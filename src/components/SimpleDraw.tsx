import * as React from 'react';

// The SimpleDraw component is the top level component for Assignment 4
const SimpleDraw: React.FC = () => {
  return (
    <p>SimpleDraw component</p>
  );
};

export default SimpleDraw;
